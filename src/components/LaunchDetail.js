import React from 'react';
import { View, Text } from 'react-native';

const LaunchDetail = ({ route }) => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>LaunchDetail!</Text>
    </View>
  );
};

export default LaunchDetail;
